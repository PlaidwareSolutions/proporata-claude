kind = "web"
previewPath = "/"
title = "HOA Operations Hub"
version = "1.0.0"
id = "artifacts/hoa-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22783

[services.development]
run = "pnpm --filter @workspace/hoa-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/hoa-hub", "run", "build" ]
publicDir = "artifacts/hoa-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22783"
BASE_PATH = "/"
